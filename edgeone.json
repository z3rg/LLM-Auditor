{
  "//": "Deploy EdgeOne Makers. API dijalankan sebagai Agent Functions (agents/), bukan Cloud Functions: mode agent memuat berkas rute sebagai modul Node biasa dengan node_modules terpasang, sehingga graf modul lib/ tidak perlu di-bundle sendiri, dan batas eksekusinya 3600 detik (bukan 120).",
  "installCommand": "npm install --omit=dev",
  "outputDirectory": "public",
  "nodeVersion": "20.18.0",
  "agents": {
    "dir": "agents",
    "framework": "openai-sdk",
    "timeout": 300,
    "externalNodeModules": [
      "@neondatabase/serverless"
    ]
  }
}
